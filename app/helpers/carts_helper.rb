module CartsHelper
  # Product.all.each { |e|
  #   if e.images.blank?
  #     image = Image.new(:product_id => e.id, :remote_image_path_url => e.image_url.to_s)
  #     e.images.push image
  #   end
  #   if e.images.length > 1
  #     e.images.last.destroy
  #   end
  # }
end
